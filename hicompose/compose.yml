version: "3.9" # obsolete so no need to write
name: "fastapi_docker_compose"
services:
  fastapi_docker_compose:
    image: fastapi_docker_compose
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: fastapi_docker_container
    ports:
      - 8000:8000
